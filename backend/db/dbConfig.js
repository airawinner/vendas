//dbConfig é um objeto que contém as informações necessárias para a conexão com o banco de dados
const dbConfig = {
    host: 'localhost',
    user: 'root',
    password: '1234',
    database: 'vendas'
};

module.exports = dbConfig;